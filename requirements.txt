# Phase 1 — no external dependencies needed (pure stdlib)
# These will be added in Phase 2 when we add embeddings + vector DB:
#
# openai>=1.0.0
# chromadb>=0.4.0




# Core — required
openai>=1.0.0        # embeddings + DeepSeek LLM
chromadb>=0.4.0      # local vector database
python-dotenv>=1.0.0 # load keys from .env file
anthropic>=0.20.0

mcp[cli]>=1.0.0